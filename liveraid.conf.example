# liveraid.conf.example
#
# Copy to /etc/liveraid.conf and edit to match your setup.

# Data drives: "data NAME DIR"
# Name must be unique; dir is the root path for that drive.
data 1 /mnt/disk1/
data 2 /mnt/disk2/
data 3 /mnt/disk3/

# Parity files: "parity LEVEL PATH"
# Level 1 = can recover 1 failed drive
# Level 2 = can recover 2 failed drives, etc. (up to 6)
# Levels must be contiguous starting from 1.
parity 1 /mnt/parity1/liveraid.parity
parity 2 /mnt/parity2/liveraid.parity

# Content file (metadata; list multiple for redundancy)
content /var/lib/liveraid/liveraid.content
content /mnt/disk1/liveraid.content

# Mount point
mountpoint /srv/array

# Block size in KiB (default 256, must be a multiple of 64 bytes when converted)
#blocksize 256

# Drive selection policy for new files: mostfree | roundrobin
#placement mostfree

# Number of threads used to drain the dirty-parity bitmap in parallel (default 1)
# Each thread handles an independent subset of dirty positions.
# Set to match the number of data drives or available CPU cores, whichever is smaller.
#parity_threads 4
